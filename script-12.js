// Crear una función que reciba 1 número,
//  imprimir si es negativo o si es positivo.

function numero(num1) {
  if (num1 > 0) {
    console.log(`El número ${num1} es positivo`);
  } else {
    console.log(`El número ${num1} es negativo`);
  }
}

numero(-8);
