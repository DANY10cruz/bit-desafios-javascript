// 5.Crear una función que reciba dos párametros,
// un nombre y un apellido y retorne el nombre completo,
// primero el apellido y luego el nombre.

function fullName(apellido, nombre) {
  return apellido + " " + nombre;
}

let ejemplo5 = fullName("Cruz", "Daniel");
console.log(ejemplo5);
